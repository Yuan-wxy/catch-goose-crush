- 核心功能模块（如 ThreeEnv, PhysicWorld, ItemPool）均采用 TypeScript Class 进行封装，提供明确的初始化、更新和销毁接口。
- 组件间通信遵循 Vue3 Composition API 规范，子组件通过 `defineEmits` 向父组件传递游戏状态（如槽位更新、通关/失败事件）。
- 3D资源管理采用对象池模式，所有动态生成的食材网格均从 `ItemPool` 获取，并在不再使用时调用 `release` 而非直接销毁。