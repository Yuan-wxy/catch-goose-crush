- 采用 Docker Compose 定义 MySQL、Redis 和 NestJS 后端服务的依赖关系与网络拓扑（`docker-compose.yml`）。
- 使用 Nginx 作为统一入口，通过 `upstream` 代理后端 API 并托管管理后台与游戏客户端的静态资源（`nginx/default.conf`）。
- 包含标准化的 NestJS 生产镜像构建流程（`Dockerfile`）及详细的分步落地执行文档（`deploy.md`）。