- 所有 API 响应均通过 `Result.success()` 或 `Result.fail()` 包装，确保返回结构统一为 {code, msg, data}。
- 业务逻辑遵循 Controller-Service 分层模式，Controller 仅负责路由映射与参数接收，具体逻辑下沉至 Service。
- 高频读取且变动少的数据（如关卡配置）在 Service 层实现“先查 Redis 后查 DB”的缓存策略。